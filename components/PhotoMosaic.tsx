import type { NextComponentType, NextPageContext } from "next";

interface Props {}

const PhotoMosaic: NextComponentType<NextPageContext, {}, Props> = (
  props: Props
) => {
  return <div></div>;
};

export default PhotoMosaic;
