import type { NextComponentType, NextPageContext } from "next";

interface Props {}

const BlogCard: NextComponentType<NextPageContext, {}, Props> = (
  props: Props
) => {
  return <div></div>;
};

export default BlogCard;
